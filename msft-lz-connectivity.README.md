# msft-eslz-connectivity

This file is kept only for historical context.

The repository is now organized as a single Terraform root module, with environment configuration under:

- `environments/prod/backend.hcl`
- `environments/prod/terraform.tfvars`

See the main documentation in `README.md` for the current architecture and how to run CI/local workflows.
